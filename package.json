{
  "name": "homebridge-mqtt-weather-sensor",
  "version": "1.0.11",
  "description": "Homebridge plugin for MQTT weather sensor",
  "main": "index.js",
  "keywords": [
    "homebridge-plugin",
    "mqtt",
    "weather"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "colussim",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^5.10.1"
  },
  "config": {
    "title": "MQTT Weather Sensor",
    "type": "object",
    "properties": {
      "host": {
        "type": "string",
        "title": "MQTT Server",
        "description": "The hostname or IP address of your MQTT server."
      },
      "port": {
        "type": "number",
        "title": "MQTT Port",
        "description": "The port for your MQTT server.",
        "default": 1883
      },
      "username": {
        "type": "string",
        "title": "Username",
        "description": "The username for the MQTT server."
      },
      "password": {
        "type": "string",
        "title": "Password",
        "description": "The password for the MQTT server.",
        "format": "password"
      },
      "topic": {
        "type": "string",
        "title": "MQTT Topic",
        "description": "The MQTT topic to subscribe to for sensor data.",
        "default": "esp32_station"
      },
      "debug": {
        "type": "boolean",
        "title": "Debug Mode",
        "description": "Enable debug mode."
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/colussim/homebridge-mqtt-weatherSensor.git"
  },
  "engines": {
    "homebridge": ">=1.3.4",
    "node": ">=14.17.0"
  }
}

