{
  "platforms": [
    {
      "platform": "MqttWeatherSensor",
      "host": "YOUR_MQTT_SERVER",          // The name or IP address of your MQTT server
      "port": 1883,                         // The port of your MQTT server (default is 1883)
      "username": "YOUR_USERNAME",          // Username for connecting to the MQTT server
      "password": "YOUR_PASSWORD",          // Password for connecting to the MQTT server
      "topic": "esp32_station"              // Topic subscribed to receive sensor data
    }
  ]
}

